PORT=5000
NODE_ENV=development

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=hotel_booking_db
DB_PORT=3306

# JWT Authentication
JWT_SECRET=hotel_booking_jwt_super_secret_key_2026_placement_ready
JWT_EXPIRES_IN=7d

# AI Service (Optional - Gemini API Key)
# If left blank, the app gracefully falls back to database-only keyword search
GEMINI_API_KEY=
